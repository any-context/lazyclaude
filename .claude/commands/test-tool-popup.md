Run this bash command to trigger a tool permission popup for testing:

```bash
echo "tool-popup-test: $(date)"
```

This should open the claude-tool-popup.js confirmation window showing the Bash tool name and the command above. Verify that the y/a/n bar appears at the bottom.