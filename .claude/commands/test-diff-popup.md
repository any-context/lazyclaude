Edit /Users/kenshin/.config/tmux/plugins.conf and toggle a test comment.
If the file contains "# write-test", remove that line.
If it does not contain "# write-test", add "# write-test" after the line "set -g @prefix_highlight_show_sync_mode 'on'".
